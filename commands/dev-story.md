---
description: Read a story file and implement it. Loads the full context (story, GDD requirement, ADR guidelines, control manifest), routes to the right programmer role, implements code and tests, and confirms each acceptance criterion. Run after /story-readiness, before /game-code-review and /story-done.
argument-hint: [task]
---

# /dev-story

Use the `dev-story` Codex skill for this request. If this workflow references
Claude-only agents, treat those names as role guidance and follow Codex's active
skills, global instructions, and local repository rules.

User arguments: $ARGUMENTS
